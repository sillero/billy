{
  "name": "billy",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-redux": "^5.0.2",
    "redux": "^3.6.0",
    "redux-cli": "^1.9.0",
    "redux-thunk": "^2.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "setup": "cd node_modules && ln -s ../src src",
    "create-component": "./bin/create-component",
    "create-module": "./bin/create-module",
    "reindex-components": "./bin/reindex-components",
    "reindex-modules": "./bin/reindex-modules"
  }
}
